# 玩法逻辑学习文档

更新时间：2026-05-21

用途：记录当前游戏已经确认的核心玩法逻辑，以及后续开发时容易和设计思路冲突的地方。后续每次改机制前，先对照本文档，避免把旧逻辑、临时测试逻辑或错误理解重新写回项目。

## 当前核心玩法定位

本游戏是“玩家攻击变异蛋、击杀指定目标、逐步打开机关和通关路径”的闯关玩法。

核心判断不再以玩家生存数值为主，而是以以下事件推进：

- 玩家真实命中变异蛋。
- 变异蛋按脚本 HP 被玩家击杀。
- 指定变异蛋组合死亡后触发门墙、蹦床、箱子或显现组件。
- 玩家完成机关交互后推进关卡。
- 玩家进入胜利区域后通关。

## 玩家数值原则

当前玩家只保留成长和攻击相关数值：

- `LV`
- `EXP`
- `ATK`

已取消的玩家数值或面板：

- 玩家脚本 HP。
- 玩家 HP 面板。
- `SPD` 面板。
- 移动速度加点。
- 手动属性点分配。

当前数值规则：

- 初始等级：`LV 0`
- 初始经验：`EXP 0`
- 初始攻击：`ATK 4`
- 0 升 1 需要 `10` 经验，之后每级需求增加 `10`。
- 最高等级 `15`。
- 每次升级固定 `ATK +2`。
- 玩家被变异蛋撞飞只表现击飞效果，不扣 HP，不触发失败。

后续不能恢复旧的“玩家血量失败机制”，除非明确重新设计失败条件。

## 攻击与伤害原则

这是当前最重要的战斗规则：

只要玩家使用任意武器、技能或道具真实命中变异蛋，就按玩家当前 `ATK` 对变异蛋造成一次伤害。

具体约束：

- 长剑、咸鱼、炸弹、突击步枪、技能等来源不能决定最终伤害值。
- 道具自身原生伤害不能造成秒杀。
- 命中一次只扣一次脚本 HP。
- 同一帧多个引擎事件同时出现时，需要按“玩家 + 怪物”去重。
- 经验获取只看“变异蛋是否被玩家按脚本 HP 击杀”，不绑定使用了什么武器或道具。
- 已经发生真实命中时，不再用地形阻挡二次否决伤害。
- 地形阻挡只限制变异蛋搜敌和追击，不限制已经发生的玩家攻击命中。

当前防秒杀机制：

- 变异蛋编辑器原生 HP 与脚本 HP 保持一致。
- 命中前把编辑器原生伤害限制为玩家当前 `ATK`。
- 原生伤害还不能超过怪物当前剩余脚本 HP。

## 变异蛋规则

当前变异蛋死亡后不复活。

| 类型 | 脚本 HP | 编辑器原生 HP | 击杀经验 | 头顶刻度 |
| --- | --- | --- | --- | --- |
| C | 10 | 10 | 4 | 每格 2 HP |
| B | 30 | 30 | 10 | 每格 6 HP |
| A | 60 | 60 | 14 | 每格 12 HP |
| 特殊变异蛋 `1491798607` | 100 | 100 | 20 | 每格 20 HP |

血条显示原则：

- 不使用编辑器默认绿色血条作为主要反馈。
- 使用头顶文本显示，例如 `HP 6/10 [❤️❤️❤️♡♡]`。
- 血条刻度必须反映当前脚本 HP。
- 如果富文本或图片血条在运行时不可控，优先保证文本 HP 和刻度准确。

AI 原则：

- 所有变异蛋，包括后来发现的 `1518591746`、`1904726297`、`1491798607` 等，都必须和同类变异蛋保持一致的 AI 行为。
- 变异蛋搜敌范围要受地图隔断影响，不能隔着方墙、机械墙、门类组件追踪玩家。
- 搜敌范围的中心判断以玩家和变异蛋之间实际距离为基础。
- 隔断组件隐藏或关闭物理后，不再作为 AI 阻挡。

当前 AI 数值：

- AI 刷新间隔：`0.4` 秒。
- 侦测范围：约 `16` 米。
- 追丢范围：约 `30` 米。
- 撞飞冷却：`2.4` 秒。
- 撞飞只表现效果，不扣玩家 HP。

## 机关与门墙原则

门墙、机关和显现组件统一以组件 ID 为准，不用等级或名称模糊判断替代。

关键原则：

- 组件 ID 是当前关卡逻辑的主要依据。
- 名称只能作为日志和兜底，不作为核心判断。
- 重叠组件必须分开处理，不允许因为位置接近就一起隐藏。
- 需要保留功能的组件，只能隐藏外观时必须明确“只隐藏外观，保留物理和功能”。
- 需要完全打开通路的门墙，必须同时隐藏模型并关闭物理阻挡。

明确规则：

- 击杀 C1 `1115853218`、C2 `1643323093`、C3 `1743679626` 后，只让 `1594492577` 消失。
- `1487962612` 穿梭电门17必须保留原功能和可见状态，不能被 `1594492577` 连带隐藏。
- 方墙46 `1925646933` 的逻辑是指定击杀后消失并关闭阻挡，不是“可穿墙”。
- 蹦床1 `1873217301` 初始隐藏时必须无弹力，但不能关闭原生物理/弹力组件；应移动到场外并隐藏外观，完成击杀条件后再复位显示。
- 需要隐身施工的功能组件，只隐藏外观，功能和物理保留。

## 密码锁原则

密码锁 `1965974982` 使用编辑器原生输入面板。

当前正确设计：

- 玩家通过编辑器原生密码面板输入密码。
- 编辑器发出解锁成功事件 `输入密码2026` 后，机械墙3 `1156059701` 才消失。
- 成功后发送提示：`大门已经打开`。
- 不再使用自建密码 UI。
- 不再使用“靠近密码锁后手动弹自建输入框”的逻辑。
- 不再把密码锁刚打开输入面板时的 `touchable=false` 当成解锁成功。

曾经冲突点：

- 输入面板打开时，密码锁可能短暂不可交互，这不是解锁成功。
- 如果监听“组件失效/不可点”过宽，会导致玩家还没输入密码，机械墙3就提前消失。
- 机械墙3必须由“解锁成功事件”驱动，而不是由靠近、打开面板、组件不可点直接驱动。

## 现有核心流程

当前执行流程按事件推进：

1. 开局播放“蛋仔圆舞曲”。
2. 玩家装备长剑，显示 `LV / EXP / ATK`。
3. 玩家击杀指定 C 组变异蛋，打开第一段门。
4. 玩家击杀指定 B 组变异蛋，打开第二段门墙。
5. 玩家击杀指定 B/A 组合，打开机械墙4和方墙46。
6. 玩家击杀 A10、A11、C9、C10 后，蹦床1显现并恢复弹力。
7. 玩家触发按钮开关1，机械墙5消失。
8. 玩家在中式花纹砖1稳定站立 3 秒，箱子1打开，实体 `1147395932` 显现。
9. 玩家击杀指定 A 组变异蛋，箱子2打开，实体 `1712673236` 显现。
10. 玩家使用编辑器原生密码锁输入正确密码，触发 `输入密码2026`，机械墙3消失。
11. 玩家进入胜利区域通关。

## 已知容易冲突的旧逻辑

后续不要把以下旧逻辑重新写回当前玩法：

- 等级达到 2 级后自动打开 `1594492577` 的旧等级门逻辑。
- 玩家 HP 被变异蛋攻击扣除并触发失败的旧机制。
- 变异蛋命中次数死亡机制。
- 变异蛋死亡后复活机制。
- 道具自身伤害决定怪物扣血的机制。
- 使用道具后按距离寻找最近怪物并直接扣血的机制。
- 突击步枪需要“3 秒激活”的旧限制。
- 方墙46旧撞击破坏机制。
- 自建密码输入 UI。
- 用密码锁 `touchable=false` 直接判定机械墙3打开。
- 隐藏功能组件时连物理和功能一起关闭。
- 对原生蹦床 `1873217301` 调用 `set_physics_active(false)` 再恢复，发布窗口里可能导致弹力功能失效。
- 使用名称或坐标模糊匹配门墙，导致重叠组件一起消失。

## 后续修改检查清单

每次改战斗、机关或关卡逻辑前，至少检查以下点：

- 是否仍然是“真实命中一次，按玩家当前 ATK 扣一次 HP”。
- 是否仍然防止道具原生伤害秒杀怪物。
- 怪物死亡后是否只发一次经验。
- 经验是否只绑定“玩家击杀怪物”，没有绑定武器类型。
- 新增变异蛋是否纳入 HP、经验、AI、血条和机关击杀监听。
- 新增门墙是否按 ID 控制，而不是只按名称或坐标。
- 隐藏组件时是否区分“隐藏外观保留功能”和“隐藏并关闭阻挡”。
- 蹦床隐藏时是否通过移动到场外实现无弹力，而不是关闭原生物理/弹力组件；显现后是否复位并可弹跳。
- 密码锁是否只监听编辑器原生解锁成功事件。
- AI 搜敌是否被可见且有物理的隔断组件阻断。
- 已经发生的玩家攻击命中是否没有被隔断逻辑错误否决。

## 当前验证结论

最近一次整体 Lua 运行循环检查结果：

- 游戏可以进入运行态。
- 没有发现 Lua 语法错误、运行时栈错误或循环崩溃。
- `Stage8To12`、密码锁事件、机关初始化和旋转组件循环均有日志。
- 当前只看到静态物理组件无法施加运动的警告，属于运动设置噪音，不是玩法主循环错误。
