# 阶段 1 到 6 进度总结

更新时间：2026-05-18

本总结以 `开发阶段拆分.md` 为验收基准，以当前 `ts_src/` 源码为依据。`project/ts_out/` 中存在旧输出残留，不作为阶段完成依据。

## 总体结论

阶段 1 到 6 已经搭出核心玩法骨架：玩家数值、怪物数值、道具命中扣怪物血、击杀经验、升级加点、怪物追击和碰撞伤害都有代码实现。

但当前还不能认为阶段 1 到 6 全部验收完成。主要缺口集中在显示同步、道具箱随机发放、唯一攻击方式约束、属性面板完整性，以及阶段 6 的游戏内实测确认。

## 阶段 1：玩家基础数值与显示

状态：部分完成。

最新更新：2026-05-18

- 玩家初始 HP 和 Max HP 已从 10 调整为 20。
- 其他升级逻辑、攻击力、移动速度、经验和属性点逻辑保持不变。
- 本次修改文件：`ts_src/PlayerStats.ts`。
- 已清理 `project/ts_out` 后重新 `npm run build`，旧残留 `project/ts_out/game/systems/CombatSystem.lua` 已移除。
- 已执行 `sync-start`，返回 `build_ok=true`、`flush_ok=true`、`main_lua_ok=true`、`start_ok=true`。

已实现：

- 玩家初始化 HP 20、Max HP 20、攻击 4、移动速度 4、等级 0、经验 0、未分配属性点 0。
- 玩家受到伤害后会从引擎 HP 同步到 `PlayerStatsData`。
- `ApplyDamage`、`Heal`、`SyncFromEngine` 会刷新玩家状态。
- HP 归零后调用 `role.lose()` 进入失败状态。

主要代码：

- `ts_src/PlayerStats.ts`
- `ts_src/main.ts`

未完成或不满足验收：

- 右上角 HP HUD 和 EXP HUD 虽然创建了 Label，但刷新时被设置为不可见，并且文本被清空。
- 文档要求“玩家头顶显示当前 HP / Max HP”，当前代码没有明确实现玩家头顶 HP 文本。
- 血量条同步目前更多是关闭或隐藏引擎血条，不符合“血量条同步降低/恢复”的验收描述。

风险：

- 阶段 1 的数值底层基本可用，但显示侧不达标，会影响后续试玩判断。

## 阶段 2：变异蛋基础数值、血量与复活

状态：基本完成，仍需游戏内验证。

最新更新：2026-05-18

- 排查并修复“变异蛋死亡后不复活”的问题。
- 根因是原实现把玩法脚本 HP 同步到了引擎 HP，导致引擎生命体进入死亡态；后续 `reborn`、`set_hp_max`、`get_hp` 等 API 在该状态下会抛内部错误，进而中断 10 秒复活回调。
- 现改为脚本掌控怪物死亡/复活：引擎 HP 维持高保底值，玩法 HP 仍使用 `MonsterData.hp`，死亡时隐藏模型并关闭物理，10 秒后回出生点、恢复满血、重新显示并启用物理。
- 同时为怪物显示、血量、AI 开关等引擎调用增加安全调用封装，避免单个 API 报错中断整条死亡/复活流程。

已实现：

- 变异蛋 C：HP 10、攻击 2、移动速度 3、击杀经验 4。
- 变异蛋 B：HP 30、攻击 4、移动速度 4、击杀经验 10。
- 变异蛋 A：HP 60、攻击 6、移动速度 5、击杀经验 14。
- 按名称和 ID 绑定了多只 A/B/C 变异蛋。
- 怪物头顶名称中附带 `当前 HP / Max HP`。
- 怪物死亡后隐藏模型、关闭物理、隐藏血条。
- 10 秒后回到出生点复活，并恢复满血。
- 禁用了引擎自动复活，改为脚本控制复活。

主要代码：

- `ts_src/MonsterManager.ts`

未完成或待确认：

- 需要在编辑器中确认所有硬编码怪物 ID 和名称都能查到。
- 需要实测 `set_model_visible(false)`、`set_physics_active(false)` 是否足够表达“死亡后消失”。
- 需要确认怪物血条显示样式是否符合玩法预期。

## 阶段 3：道具箱与唯一攻击方式

状态：部分完成。

最新更新：2026-05-18

- 长剑已单独增加脚本攻击间隔变量 `SWORD_ATTACK_INTERVAL = 0.35` 秒。
- 该间隔只作用于起始长剑的 `sword` 伤害分支，不影响咸鱼、突击步枪、炸弹的命中次数、持续时间和伤害结算。
- 本次修改文件：`ts_src/ItemAttackManager.ts`。
- 已确认生成的 `project/ts_out/game/ItemAttackManager.lua` 包含 `SWORD_ATTACK_INTERVAL = math.tofixed(0.35)`。
- 已执行 `sync-start`，返回 `build_ok=true`、`flush_ok=true`、`main_lua_ok=true`、`start_ok=true`。

手感调整：2026-05-18

- 长剑后摇仍偏长，已将 `SWORD_ATTACK_INTERVAL` 从 `0.35` 秒缩短到 `0.2` 秒。
- 攻击计时 tick 从 `0.05` 秒缩短到 `0.02` 秒，减少计时粒度造成的手感延迟。
- 其他道具和玩家升级/攻击力逻辑不变。
- 已执行 `npm run build` 和 `sync-start`，均成功。
- 已确认生成的 `project/ts_out/game/ItemAttackManager.lua` 包含 `SWORD_ATTACK_INTERVAL = math.tofixed(0.2)`。

起始武器调整：2026-05-18

- 玩家起始武器已从长剑 `6000035` 改为突击步枪 `6000026`。
- 本次修改文件：`ts_src/PlayerWeaponManager.ts`。
- 已执行 `npm run build` 和 `sync-start`，均成功。
- 已确认生成的 `project/ts_out/game/PlayerWeaponManager.lua` 包含 `STARTING_WEAPON_KEY = 6000026`。

已实现：

- 配置了三种攻击道具：
  - 咸鱼 `6000033`，有效命中 3 次。
  - 突击步枪 `6000026`，主动使用后有效 3 秒。
  - 炸弹 `6000029`，有效命中 1 次。
- 监听玩家获得装备事件，并登记装备归属。
- 监听装备使用事件，记录装备激活状态。
- 怪物受击时通过拦截器判断伤害来源。
- 有效命中后按玩家当前攻击力扣除怪物 HP。
- 有效命中次数耗尽后销毁道具。
- 对未授权的引擎伤害有回滚/阻断逻辑，目标是避免普通碰撞或非脚本来源伤害怪物。
- 起始长剑脚本伤害现在有单独的 0.2 秒攻击间隔。

主要代码：

- `ts_src/ItemAttackManager.ts`
- `ts_src/MonsterManager.ts`

未完成或不满足验收：

- 道具箱随机发放逻辑没有完整接管。当前 `SPEC_LIFEENTITY_GET_ITEMBOX` 后主要是延迟读取玩家当前选中装备，而不是脚本明确随机发放三种道具之一。
- `GrantRandomAttackItem` 已存在，但没有接到道具箱拾取主流程。
- 当前 `handleMonsterEngineHit` 在没有道具状态时，如果玩家有起始武器，也允许造成 `sword` 伤害。这和执行文档“玩家只能通过道具箱随机获得攻击道具”“唯一攻击方式”冲突。
- 突击步枪的“3 秒内有效”依赖使用事件和激活状态，但命中判断里 fallback 到当前选中道具，严格性还需要复查。

风险：

- 如果引擎本身道具箱会随机产出非目标装备，当前逻辑可能无法保证只产出咸鱼、突击步枪、炸弹。
- 起始武器系统会破坏阶段 3 的唯一攻击方式验收。

## 阶段 4：击杀归属、经验与升级

状态：基本完成。

最新更新：2026-05-18

- 排查并修复“击杀变异蛋未获得经验”的问题。
- 根因是引擎死亡事件里先尝试恢复引擎死亡，再执行授权攻击拦截；当恢复 API 报错时，脚本伤害和击杀经验结算被提前中断。
- 现已调整为：引擎死亡事件先进入道具/长剑授权伤害拦截，若脚本 HP 归零则立即执行 `killMonster` 和 `PlayerStats.AddExp`；只有未被脚本击杀时才恢复引擎死亡状态。
- 本次启动日志已观察到 `变异蛋C1` 被击杀后打印 `add exp role=1 gain=4` 和 `kill exp 变异蛋C1 ... exp=4`，经验结算链路已恢复。

已实现：

- 怪物被玩家脚本伤害时记录最后命中玩家。
- 怪物死亡时按怪物类型发放击杀经验。
- C/B/A 分别发放 4/10/14 经验。
- 升级经验表为 `(当前等级 + 1) * 10`，即 0 升 1 需要 10，14 升 15 需要 150。
- 最高等级限制 15。
- 每升 1 级增加 2 点属性点。
- 支持超额经验连续升级。
- 经验变化后调用 `RefreshDisplay`。

主要代码：

- `ts_src/PlayerStats.ts`
- `ts_src/MonsterManager.ts`

未完成或待确认：

- EXP HUD 显示目前被隐藏，所以“经验值 HUD 同步更新”的显示验收不通过。
- 达到满级后当前代码会把经验清零，是否符合设计需要确认。

## 阶段 5：属性面板与加点

状态：部分完成。

已实现：

- 代码创建了属性面板相关 UI 节点。
- 显示内容包含 EXP、HP、ATK、SPD。
- 有 HP、攻击、移动速度三个加点按钮。
- HP 加点：Max HP +1，并补 1 点当前 HP。
- 攻击加点：攻击 +1。
- 移动速度加点：移动速度 +1。
- 未分配属性点不足时拒绝加点。
- 加点后刷新面板和实际属性。

主要代码：

- `ts_src/PlayerStats.ts`

未完成或不满足验收：

- 面板没有明确显示等级。
- 面板没有明确显示未分配属性点数量。
- 按钮文本被清空，按钮本身依赖子 Label 显示，交互可用性需要实测。
- `refreshPanelButton` 忽略传入的 `enabled` 参数，始终 `set_button_enabled(button, true)`，虽然 `AddAttributePoint` 会兜底拒绝，但 UI 状态不符合“未分配属性点不足时不能继续加点”的直观表现。
- 提示文本有英文，例如 `No attribute points`、`Point added`，需要统一成中文。

风险：

- 如果 EUI 事件回传数据里没有 `role` 字段，加点按钮可能无法定位玩家。

## 阶段 6：变异蛋追击、越障与冲撞攻击

状态：部分完成，必须游戏内实测。

最新更新：2026-05-18

- 已优化变异蛋对玩家的冲撞伤害条件。
- 原逻辑是追击状态下接触玩家就扣 HP。
- 新逻辑是接触时先对玩家施加击飞冲击，短延迟后检查玩家是否产生明显位移、速度或向上位移；只有确认被撞飞才扣 HP。
- 本次修改文件：`ts_src/MonsterManager.ts`。
- 已执行 `npm run build`，编译通过。
- 已执行 `sync-start`，返回 `build_ok=true`、`flush_ok=true`、`main_lua_ok=true`、`start_ok=true`。
- 已确认 `project/ts_out/game/MonsterManager.lua` 包含新日志 `collision knock check` / `knock attack`，说明 TS 已正确编译到 Lua。
- 已通过 `game-start` 看到 12:15:30 后的新运行时入口日志；本轮未远程操控玩家触发实际碰撞，因此击飞扣血手感仍待实机验证。

攻击频率调整：2026-05-18

- 变异蛋冲撞攻击频率过高，已降低攻击频率。
- 单只变异蛋的冲撞攻击冷却从 `1.2` 秒提高到 `2.4` 秒。
- 冷却实现从依赖脚本 AI 计数 `aiClock` 改为真实延迟锁 `collisionAttackCooling`，避免接触事件密集触发时绕过冷却。
- 伤害数值、追击逻辑、击飞成功才扣 HP 的条件不变。
- 已执行 `npm run build` 和 `sync-start`，均成功。
- 已确认生成的 `project/ts_out/game/MonsterManager.lua` 包含 `AI_ATTACK_COOLDOWN = math.tofixed(2.4)` 和 `collisionAttackCooling`。

已实现：

- 怪物初始化时启动 AI，然后由脚本周期性控制追击/返回/待机。
- 玩家进入侦测范围后，怪物追击玩家。
- 玩家离开或视线被阻挡后，怪物返回出生点。
- 怪物碰撞玩家时会先尝试击飞玩家，确认击飞成立后按怪物攻击力扣除玩家 HP。
- A/B/C 冲撞伤害分别来自配置值 6/4/2。
- 对碰撞攻击设置了冷却，避免短时间重复扣血。
- 追击逻辑会用射线和场景障碍判断，避免隔墙追击。

主要代码：

- `ts_src/MonsterManager.ts`

未完成或待确认：

- “移动过程中的自动越障/跳跃寻路”目前依赖引擎移动能力，没有看到明确的越障/跳跃控制或实测结果。
- 文档要求“禁止变异蛋原地定时跳跃”，当前没有显式定时跳跃逻辑，这点代码上满足，但仍需试玩观察。
- 冲撞伤害现在要求“接触后成功击飞玩家”，但击飞力度、位移阈值和速度阈值仍需游戏内确认手感。
- 如果接触事件的 `unit2` 字段在运行时不稳定，可能导致碰撞伤害不触发。

## 后续建议

建议先补齐阶段 1、3、5 的明确验收缺口，再推进阶段 7 之后的机关系统：

1. 修复玩家 HP/EXP HUD 和头顶 HP 显示。
2. 明确道具箱由脚本随机发放三种攻击道具，并移除起始武器对怪物造成伤害的通路。
3. 完善属性面板显示等级、经验、HP、攻击、移动速度、未分配属性点，并让无属性点时按钮禁用。
4. 对阶段 6 做一次游戏内实测，记录追击、返回、越障、碰撞伤害是否符合预期。
