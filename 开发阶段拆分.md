# 变异蛋闯关开发阶段拆分

更新时间：2026-05-21

本文档只记录当前有效机制。旧的玩家 HP 控制、`SPD` 属性、移动速度加点、命中次数死亡、怪物复活、等级门等机制均不再作为当前开发依据。

当前入口版本：

- `ts_src/main.ts`：`build=20260521_password_success_event_v1`
- TypeScript 源码目录：`ts_src/`
- Lua 输出目录：`project/ts_out/`

## 当前模块入口

`ts_src/main.ts` 在 `GAME_INIT` 时按顺序初始化：

1. `BgmManager.Init()`
2. `PlayerStats.InitAllPlayers()`
3. `MonsterManager.InitAllMonsters()`
4. `ItemAttackManager.Init()`
5. `PlayerWeaponManager.EquipStartingWeaponForAllPlayers()`
6. `PlayerSkillManager.EquipPlayerPresetSkillForAllPlayers()`
7. `Stage7GateSystem.Init()`
8. `Stage8To12System.Init()`

## 阶段 0：开局音乐

实现文件：`ts_src/BgmManager.ts`

当前职责：

- 游戏开始后延迟 2 帧播放循环音乐“蛋仔圆舞曲”。
- 使用触发区域 `1648649203`（通用触发区域1）作为开局音乐配置点和日志定位。
- 当前测试 SoundKey 为 `2001`，优先使用全局 2D 循环音频接口；如果全局 2D 接口不可用，则尝试对当前有效玩家播放 2D 音频；仍不可用时，在触发区域位置播放 3D 音频。

## 阶段 1：玩家数值与属性 UI

实现文件：`ts_src/PlayerStats.ts`

当前职责：

- 初始化玩家等级、经验和攻击。
- 创建并刷新属性 UI：`LV`、`EXP`、`ATK`。
- 处理经验、升级和自动攻击成长。
- 保持玩家 HP 控制关闭。

当前规则：

- 初始等级：`0`
- 初始经验：`0`
- 初始攻击：`4`
- 升级经验：`(当前等级 + 1) * 10`
- 最高等级：`15`
- 每升 1 级固定 `ATK +2`。
- 不创建或刷新 `HP` UI。
- 不创建或刷新 `SPD` UI。
- 不保存 `moveSpeed`，不通过脚本修改玩家移动速度。
- 不保留手动属性点分配和攻击加点按钮。
- `ApplyDamage()` 只打印忽略日志，不扣玩家 HP，不触发失败。

关键日志：

- `[Stage1][PlayerStats] InitAllPlayers build=20260519_auto_atk_upgrade_v1`
- `[Stage5][PlayerStats] stats panel created mode=no_hp_no_spd`
- `[Stage1][PlayerStats] init ... hpControl=disabled ... spdPanel=disabled`
- `[Stage4][PlayerStats] add exp ...`

## 阶段 2：变异蛋 HP、死亡和经验

实现文件：`ts_src/MonsterManager.ts`

当前职责：

- 初始化所有变异蛋。
- 维护脚本侧怪物 HP。
- 在变异蛋头顶显示单行 HP 数值和爱心刻度血条；C 每个刻度代表 `2` HP，B 每个刻度代表 `6` HP，A 每个刻度代表 `12` HP，特殊变异蛋每个刻度代表 `20` HP。
- 关闭编辑器默认绿色血条，只保留头顶 HP 数值和爱心刻度。
- 测试中：所有 C 类变异蛋额外绑定 ABC 样式场景 UI 血条图层 `1073741933`。
- 处理授权攻击扣血、死亡、击杀归属和经验。
- 经验发放只看一条规则：变异蛋被玩家按脚本 HP 击杀时，给该玩家发放对应经验。
- 经验不绑定长剑、技能、道具箱武器或其它攻击来源。
- 任意玩家攻击命中一次，只按玩家当前 `ATK` 扣除一次变异蛋脚本 HP；命中来源统一监听玩家造成伤害和道具实体碰撞事件。
- 单人试玩时，如果命中事件没有直接带出玩家归属，会兜底归属到当前唯一有效玩家，仍按玩家当前 `ATK` 扣除怪物 HP。
- 命中事件已经发生时，不再按武器、道具、技能来源或攻击间隔阻断扣血；每次有效命中事件都扣一次对应玩家当前 `ATK`。
- 当前命中入口包括：玩家生命体 `SPEC_LIFEENTITY_DMG_AFTER`、怪物受伤 `SPEC_LIFEENTITY_DMGED_AFTER`、道具实体 `SPEC_OBSTACLE_CONTACT_BEGIN`；运行时不支持的 `ABILITY_BULLET_HIT` 注册已禁用，避免启动时报 `event name is invalid`。
- 怪物受伤前还会监听 `SPEC_LIFEENTITY_DMGED_BEFORE`，把编辑器原生伤害限制为玩家当前 `ATK`。
- 同一次命中如果同时派发多个事件，会按玩家和怪物做一帧去重，只扣一次脚本 HP。
- 变异蛋编辑器原生 HP 与脚本 HP 保持一致：C/B/A 分别为 `10/30/60`。
- 原生伤害在命中前会被改写为玩家当前 `ATK`，并且不超过怪物当前剩余脚本 HP，避免道具自带高伤害秒杀。
- 死亡后通知机关系统。
- 维持怪物死亡后不复活。

怪物配置：

| 类型 | HP | 攻击 | 移速 | 击杀经验 | 复活 |
| --- | --- | --- | --- | --- | --- |
| C | 10 | 2 | 3 | 4 | 不复活 |
| B | 30 | 4 | 4 | 10 | 不复活 |
| A | 60 | 6 | 5 | 14 | 不复活 |
| 特殊 | 100 | 6 | 5 | 20 | 不复活 |

当前怪物 ID：

| 类型 | 组件 ID |
| --- | --- |
| A | A3 `1904726297`、A4 `2105425904`、A6 `1526475831`、A7 `1303708057`、A8 `1327462744`、A10 `1135616291`、A11 `1549045028`、A12 `1073930844`、A13 `1731296382`、A14 `1997721046` |
| B | B5 `1881048733`、B6 `1080764999`、B7 `1760381794`、B10 `1502667218`、B11 `2115675148`、B12 `1101855552`、B13 `1854567188` |
| C | C1 `1115853218`、C2 `1643323093`、C3 `1743679626`、C5 `2041243566`、C6 `1784268053`、C7 `1078008687`、C9 `1062383567`、C10 `1512540079`、C11 `1534008757` |

额外运行时变异蛋：

- `1518591746`：按 A 类变异蛋赋予同类 HP、经验、追击和撞飞 AI。
- `1491798607`：HP 为 `100`，击杀经验为 `20`，搜敌、追击和撞飞攻击方式与其它变异蛋一致。
- 运行时还会扫描场景里所有名称以 `变异蛋A/B/C` 开头的生命体，自动纳入同类 HP、经验、血条和 AI。

关键日志：

- `[Stage2][MonsterManager] init ... hp=10/10 ... killExp=4`
- `[Stage2][MonsterManager] native damage clamped ... atk=... native=...`
- `[Stage3][MonsterManager] authorized hit ... damage=... hp=...`
- `[Stage4][MonsterManager] kill exp ...`
- `[Stage2][MonsterManager] dead ... revive=disabled`

## 阶段 3：武器、技能、道具与攻击归属

实现文件：

- `ts_src/PlayerWeaponManager.ts`
- `ts_src/PlayerSkillManager.ts`
- `ts_src/ItemAttackManager.ts`

当前职责：

- 给玩家开局装备长剑。
- 给玩家开局装备预设技能。
- 追踪道具箱攻击道具。
- 将长剑、技能、道具的有效命中统一转为怪物扣 HP。

当前规则：

- 起始长剑：EquipmentKey `6000035`。
- 预设技能：AbilityKey `10040`，技能槽 `1`。
- 有效命中按玩家当前 `ATK` 扣除怪物 HP。
- 命中事件已经发生时，不再用方墙等场景障碍做二次否决。
- 命中事件已经发生时，不再按攻击来源或武器间隔阻断扣血。
- 命中入口为玩家造成伤害事件、怪物受伤事件和道具实体碰撞事件；不再用“使用道具后查找最近怪物”的距离兜底结算伤害。
- 怪物原生受伤前会把编辑器原生伤害改写为玩家当前 `ATK`，防止道具自带高伤害秒杀。
- 同一次命中如果同时触发多条引擎事件，会按玩家和怪物做一帧去重，只扣一次脚本 HP。

道具配置：

| 道具 | EquipmentKey | 规则 |
| --- | --- | --- |
| 咸鱼 | `6000033` | 最多 3 次有效命中 |
| 突击步枪 | `6000026` | 无 3 秒激活限制，默认最多 30 次有效命中 |
| 炸弹 | `6000029` | 最多 1 次有效命中 |

追踪道具箱：

- `道具箱16`
- `道具箱15`
- `道具箱14`
- `道具箱13`
- `道具箱12`
- `道具箱11`
- `道具箱10`
- `道具箱9`
- `道具箱8`
- `道具箱1`

关键日志：

- `[Stage6][PlayerWeapon] equipped starting weapon ... key=6000035`
- `[Stage7][PlayerSkill] equipped preset skill ... key=10040`
- `[Stage3][ItemAttack] outgoing native damage clamped ... atk=... native=...`
- `[Stage3][ItemAttack] hit-event damage ... attack=...`
- `[Stage3][Probe] equipment contact ... result=...`
- `[Stage3][ItemAttack] ability bullet hit event disabled ... fallback=lifeentity_damage_events`

## 阶段 4：变异蛋追击与撞飞效果

实现文件：`ts_src/MonsterManager.ts`

当前职责：

- 让变异蛋侦测、追击和返回。
- 处理变异蛋碰撞玩家后的击飞效果。
- 保证变异蛋攻击不再扣玩家 HP。

当前规则：

- AI 刷新间隔：`0.4` 秒。
- 侦测范围：约 `16` 米。
- 搜敌先判断距离，再判断玩家到变异蛋之间是否被隔断组件阻挡；当前纳入 `方墙`、`机械墙`、`欧式方形窄门`、`穿梭电门` 前缀，且已隐藏的组件不再作为阻挡。
- 门墙类机关消失时同步关闭模型显示、玩家可见性和物理；AI 隔断判定会忽略物理已关闭的组件，避免已打开门墙继续挡住变异蛋搜敌。
- 隔断判定只影响变异蛋搜敌、追击和撞飞前视线确认，不否决已经发生的玩家武器或道具命中事件。
- 丢失范围：约 `30` 米。
- AI 刷新遍历实际已初始化的变异蛋列表，包含静态表、额外 ID 和场景扫描纳入的变异蛋。
- 撞飞冷却：`2.4` 秒。
- 碰撞后先施加击飞力，再延迟 `0.18` 秒检查是否真的被撞飞。
- 确认撞飞后只提示撞飞和打印日志，`damage=0`。

关键日志：

- `[Stage6][MonsterManager] script chase ...`
- `[Stage6][MonsterManager] script return ...`
- `[Stage6][MonsterManager] collision knock check ...`
- `[Stage6][MonsterManager] knock effect ... damage=0`

## 阶段 5：指定击杀门墙

实现文件：`ts_src/Stage7GateSystem.ts`

当前职责：

- 监听 `MonsterManager.OnMonsterKilled()`。
- 记录指定怪物击杀进度。
- 按 ID 控制门墙隐藏和物理关闭。
- 保留穿梭电门17功能。

当前规则：

| 击杀条件 | 目标组件 | ID | 行为 |
| --- | --- | --- | --- |
| C1 `1115853218`、C2 `1643323093`、C3 `1743679626` | 欧式方形窄门1 | `1594492577` | 隐藏 |
| 同上 | 穿梭电门17 | `1487962612` | 保持可见 |
| 同上 | 显现组件1548549609 | `1548549609` | 初始隐藏，条件达成后显现 |
| B13 `1854567188`、B7 `1760381794`、B6 `1080764999`、B5 `1881048733` | 欧式方形窄门2 | `1050532163` | 隐藏 |
| 同上 | 机械墙6 | `1687330069` | 隐藏 |
| B5 `1881048733`、B6 `1080764999`、B7 `1760381794` | 显现组件1435881969 | `1435881969` | 初始隐藏，条件达成后显现 |
| B12 `1101855552`、B11 `2115675148`、B10 `1502667218`、A4 `2105425904` | 机械墙4 | `1381759788` | 隐藏 |
| 同上 | 方墙46 | `1925646933` | 隐藏模型并关闭阻挡 |

关键日志：

- `[Stage7][GateSystem] monster killed ...`
- `[Stage7][GateSystem] visible target name=... visible=false`
- `[Stage7][GateSystem] unlock ... trigger=monster_kill_progress`

## 阶段 6：阶段 8 到 12 机关系统

实现文件：

- `ts_src/Stage8To12System.ts`
- `ts_src/RotatingUnitManager.ts`

当前职责：

- 管理蹦床1显现；蹦床初始隐藏并关闭物理，显现时复位到原坐标并启用弹力。
- 管理按钮开关1和机械墙5。
- 管理中式花纹砖1、箱子1和实体1147395932。
- 管理箱子2和实体1712673236。
- 管理密码锁1原生解锁成功事件、机械墙3隐藏和胜利区域1；监听编辑器事件 `输入密码2026` 后隐藏机械墙3，并发送 `机械墙3消失` 停用密码锁互动；启动时不创建自建密码 UI；密码锁打开原生输入面板期间的不可交互状态不再作为开门条件。
- 管理拖尾电流 `1994453219` 以及 `1570439517`、`2034104046`、`1706126085`、`1850114966`、`1085615249`、`1887047585`、`1430735542`、`1422072101`、`1524647113` 的视觉隐藏，保留物理和功能。
- 管理实体 `1077691945`、`1275660450`、`1453043027` 的转动速度。

当前机关：

| 机关 | 组件 | ID | 当前行为 |
| --- | --- | --- | --- |
| 方墙32 | 方墙32 | `1523845553` | 旧等级提示已清空 |
| 蹦床1 | 蹦床1 | `1873217301` | 初始隐藏并关闭弹力/物理；击杀 A10、A11、C9、C10 后复位到原坐标，显示并启用弹力 |
| 按钮开关1 | 按钮开关1 | `1855557960` | 玩家靠近后触发 |
| 机械墙5 | 机械墙5 | `1732139556` | 按钮触发后隐藏 |
| 中式花纹砖1 | 中式花纹砖1 | `1041548108` | 稳定站立 3 秒触发；移动或被击飞重置计时 |
| 箱子1 | 箱子1 | `1192156288` | 站砖触发后隐藏 |
| 箱子1显现实体 | 实体1147395932 | `1147395932` | 箱子1打开后显现 |
| 箱子2 | 箱子2 | `1218997392` | 击杀 A7、A14、A8、A13 后隐藏 |
| 箱子2显现实体 | 实体1712673236 | `1712673236` | 箱子2打开后显现 |
| 密码锁1 | 密码锁1 | `1965974982` | 使用编辑器原生密码输入面板；监听编辑器“解锁成功事件”`输入密码2026`；保留组件丢失、模型隐藏或物理失效兜底 |
| 机械墙3 | 机械墙3 | `1156059701` | 收到 `输入密码2026` 后隐藏，并发送 `机械墙3消失` 给编辑器互动停用事件；不使用自建密码 UI |
| 胜利区域1 | 胜利区域1 | `1146983526` | 进入触发通关 |
| 拖尾电流 | 拖尾电流 | `1994453219` | 开局仅隐藏外观，保留物理和功能 |
| 隐身功能组件 | 隐身功能组件 | `1570439517`、`2034104046`、`1706126085`、`1850114966`、`1085615249`、`1887047585`、`1430735542`、`1422072101`、`1524647113` | 开局仅隐藏外观，保留物理和功能 |
| 旋转组件 | 实体1077691945 | `1077691945` | 开局设置转动速度为 `10.0` |
| 旋转组件 | 实体1275660450 | `1275660450` | 开局设置转动速度为 `2.5` |
| 旋转组件 | 实体1453043027 | `1453043027` | 开局设置转动速度为 `20.0` |

蹦床1条件：

- A10 `1135616291`
- A11 `1549045028`
- C9 `1062383567`
- C10 `1512540079`

箱子2条件：

- A7 `1303708057`
- A14 `1997721046`
- A8 `1327462744`
- A13 `1731296382`

关键日志：

- `[Stage8To12] functional visual 蹦床1 id=1873217301 visible=true reason=stage8 target monsters killed pos=(...) physics=keep_active`
- `[Stage9] button triggered ... opened=true`
- `[Stage10] tile movement reset ...`
- `[Stage10] box1 open ... reveal=1147395932`
- `[Stage11] box2 open=true reveal=1712673236`
- `[Stage12] victory ...`

## 当前通关链路

1. 玩家开局获得长剑和预设技能。
2. 击杀 C1、C2、C3，打开欧式方形窄门1。
3. 击杀 B13、B7、B6、B5，打开欧式方形窄门2和机械墙6。
4. 击杀 B12、B11、B10、A4，隐藏机械墙4和方墙46。
5. 击杀 A10、A11、C9、C10，显示蹦床1。
6. 靠近按钮开关1，打开机械墙5。
7. 在中式花纹砖1上稳定站立 3 秒，打开箱子1并显示实体1147395932。
8. 击杀 A7、A14、A8、A13，打开箱子2并显示实体1712673236。
9. 进入胜利区域1，触发通关。

## 当前验收重点

- `LV / EXP / ATK` 显示正常。
- 不出现玩家脚本 `HP` UI。
- 不出现 `SPD` UI。
- 升级后每升 1 级固定 `ATK +2`。
- 玩家 ATK 能正确影响变异蛋扣血。
- C/B/A 怪物 HP 与击杀经验正确。
- 怪物死亡后不复活。
- 怪物撞飞玩家不扣 HP。
- 所有门墙、蹦床、箱子和通关机关按 ID 条件触发。

## 已处理问题记录

- 已接入变异蛋 AI 隔断组件判定：玩家位置是搜敌范围的中心参考，`方墙`、`机械墙`、`欧式方形窄门`、`穿梭电门` 等组件代表地图隔断；如果玩家到变异蛋的路径被可见隔断组件阻挡，则搜敌不成立，并中断追击，避免穿墙搜敌、隔墙追击和卡地形问题。
- 阻挡判定只用于搜敌/追击视线，不用于否决已经发生的武器或道具命中事件；命中伤害仍按玩家当前 ATK 结算。
- 实测日志观察点：方墙阻挡生效时会节流输出 `[Stage6][MonsterManager] sight blocked ...`。
