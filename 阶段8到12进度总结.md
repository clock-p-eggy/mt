# 阶段 8 到 12 进度总结

本总结对应 `开发阶段拆分.md` 的阶段 8 到阶段 12。

## 2026-05-19 历史记录：命中次数机制

本节是历史记录，后续已恢复攻击、等级与经验数值，不作为当前玩法依据：

- 玩家数值体系取消：不再使用脚本 HP、经验、等级、攻击力、移动速度、属性点。
- 屏幕数值面板取消：不再创建或刷新 `LV`、`EXP`、`HP`、`ATK`、`SPD`。
- 任意能解析到玩家归属的武器、道具、技能命中变异蛋，都会造成一次命中结果。
- 变异蛋改为命中次数死亡：A 需 4 次，B 需 3 次，C 需 2 次。
- 变异蛋死亡后不再复活。
- 变异蛋追击、撞飞行为保留，但撞飞玩家不再扣 HP，只表现击飞效果。
- 原等级门不再监听等级，改为监听指定怪物击杀进度。
- 击杀 C1 `1115853218`、C2 `1643323093`、C3 `1743679626` 后，`1594492577` 消失，`1487962612` 保持可见和原功能。
- 击杀 B13 `1854567188`、B7 `1760381794`、B6 `1080764999`、B5 `1881048733` 后，`1050532163`、`1687330069` 消失。
- 方墙46不再使用等级条件；后续已改为指定击杀条件控制。
- 方墙32旧等级提示已清空。
- Stage10 保持当前逻辑：中式花纹砖1连续站立 3 秒后箱子1打开，实体 `1147395932` 显现。

已改动文件：

- `ts_src/PlayerStats.ts`：缩减为玩家运行态注册与旧 UI 隐藏，不再维护数值。
- `ts_src/MonsterManager.ts`：怪物改为命中次数死亡，死亡后不复活，撞飞不扣玩家血。
- `ts_src/ItemAttackManager.ts`：所有玩家归属命中都可触发一次怪物命中。
- `ts_src/Stage7GateSystem.ts`：原等级门改为指定怪物击杀后打开/隐藏。
- `ts_src/Stage8To12System.ts`：方墙32清空旧等级提示。
- `ts_src/main.ts`：入口 marker 更新为 `20260519_hit_count_no_player_stats_v1`。

验证结果：

- 已执行 `npm run build`，编译通过。
- 已执行 `sync-start`，返回 `build_ok=true`、`flush_ok=true`、`main_lua_ok=true`、`start_ok=true`。
- 编辑器日志确认入口加载：`[mt] TypeScript entry loaded build=20260519_hit_count_no_player_stats_v1`。
- 冷启动日志确认不再创建数值面板：`[Stage1][PlayerRuntime] init role=1 statsPanel=disabled`。
- 冷启动日志确认开门系统进入击杀进度模式：`mode=monster_kill_progress`。
- 冷启动日志确认方墙32旧提示已清空：`text=`。
- 命中次数验证通过：
  - C1 第二次命中死亡。
  - B5 第三次命中死亡。
  - A3 第四次命中死亡。
- 玩家伤害验证通过：调用玩家伤害后日志为 `ignore player damage`，不触发失败。

## 2026-05-19 指定击杀开门与不复活调整

本节为指定击杀开门规则基础，后续同日更新以更靠下的“当前最新规则”为准：

- 变异蛋死亡后不再复活，死亡日志为 `revive=disabled`，不再出现 `revive ... hp=...`。
- `1594492577` 的消失条件改为击杀 C1 `1115853218`、C2 `1643323093`、C3 `1743679626`。
- `1487962612` 保留原功能，在 `1594492577` 消失时会显式保持可见。
- `1050532163`、`1687330069` 的消失条件改为击杀 B13 `1854567188`、B7 `1760381794`、B6 `1080764999`、B5 `1881048733`。
- `Stage7GateSystem` 初始化时不再开局隐藏上述门墙，只把它们恢复为可见的待解锁状态。

已验证：

- `npm run build` 通过。
- `sync-start` 返回 `build_ok=true`、`flush_ok=true`、`main_lua_ok=true`、`start_ok=true`。
- fresh logs 确认开门系统进入 `mode=monster_kill_progress`，且没有旧的 `trigger=no_player_level`。
- fresh logs 确认开局 `1594492577`、`1050532163`、`1687330069` 都为 `visible=true`。
- 击杀 C1、C2 后不会打开 `1594492577`；击杀 C3 后日志出现：
  - `visible target name=欧式方形窄门1 id=1594492577 visible=false`
  - `visible target name=穿梭电门17 id=1487962612 visible=true reason=preserve overlap`
- 击杀 B13、B7、B6、B5 后日志出现：
  - `visible target name=欧式方形窄门2 id=1050532163 visible=false`
  - `visible target name=机械墙6 id=1687330069 visible=false`
- 等待超过 10 秒后日志中没有 `[Stage2][MonsterManager] revive `，确认死亡怪物不会复活。

## 2026-05-19 长剑与机关击杀条件调整

本节为机关击杀条件基础，后续同日更新以更靠下的“当前最新规则”为准：

- 玩家开局武器改为长剑，EquipmentKey：`6000035`。
- 击杀 B12 `1101855552`、B11 `2115675148`、B10 `1502667218`、A4 `2105425904` 后，`1381759788`、`1925646933` 消失。
- 保留 `1855557960` 控制 `1732139556` 的机制：玩家靠近按钮开关1后机械墙5隐藏。
- `1873217301` 初始隐藏；击杀 A10 `1135616291`、A11 `1549045028`、C9 `1062383567`、C10 `1512540079` 后显现。
- `1041548108` 站立 3 秒逻辑增加移动/被击飞重置：计时期间玩家位置变化或线速度过高会重新计时。
- `1218997392` 让 `1712673236` 显现的条件改为击杀 A7 `1303708057`、A14 `1997721046`、A8 `1327462744`、A13 `1731296382`。

已验证：

- `npm run build` 通过。
- `sync-start` 返回 `build_ok=true`、`flush_ok=true`、`main_lua_ok=true`、`start_ok=true`。
- fresh logs 确认入口加载：`[mt] TypeScript entry loaded build=20260519_sword_kill_gate_v2`。
- fresh logs 确认起始武器：`[Stage6][PlayerWeapon] equipped starting weapon role=1 key=6000035`。
- fresh logs 确认 `1712673236` 存在并初始化隐藏：`hide 实体1712673236 id=1712673236 reason=stage11 init hidden`。
- 自动击杀验证通过：
  - B12、B11、B10、A4 全部击杀后，日志确认 `1381759788`、`1925646933` 均 `visible=false`。
  - A10、A11、C9、C10 全部击杀后，日志确认 `show 蹦床1 id=1873217301 reason=stage8 target monsters killed`。
  - A7、A14、A8、A13 全部击杀后，日志确认 `hide 箱子2 id=1218997392`，并 `show 实体1712673236 id=1712673236`。
- 按钮验证通过：玩家移动到按钮开关1附近后日志确认 `[Stage9] button triggered role=1 opened=true`。
- 花纹砖验证通过：玩家站砖期间移动后日志确认 `[Stage10] tile movement reset ...`，稳定站立后日志确认 `[Stage10] box1 open role=1 reveal=1147395932`。

## 2026-05-19 恢复攻击、等级与经验数值

本节为攻击、等级与经验恢复记录；后续同日更新以更靠下的“当前最新规则”为准：

- 玩家恢复等级、经验、攻击力、移动速度和属性点数值。
- 玩家血量控制仍取消：脚本不控制玩家 HP，不创建或刷新 HP 面板，变异蛋撞飞玩家不扣 HP、不触发失败。
- 玩家初始等级 `0`、经验 `0`、攻击 `4`、移动速度 `4`、属性点 `0`。
- 经验规则恢复：0 升 1 需要 `10`，之后每级需求增加 `10`，最高 `15` 级；每次升级获得 `2` 点属性点。
- 属性点当前只加攻击或移动速度，不再提供 HP 加点。
- 变异蛋从命中次数死亡改回 HP 扣血死亡：
  - C：HP `10`，击杀经验 `4`。
  - B：HP `30`，击杀经验 `10`。
  - A：HP `60`，击杀经验 `14`。
- 所有能解析到玩家归属的武器、道具、技能命中变异蛋，都会按玩家当前 `ATK` 扣除怪物 HP。
- 变异蛋死亡后仍不复活，指定击杀开门、蹦床和箱子2显现逻辑保持不变。

已验证：

- `npm run build` 通过。
- `sync-start` 返回 `build_ok=true`、`flush_ok=true`、`main_lua_ok=true`、`start_ok=true`。
- fresh logs 确认入口加载：`[mt] TypeScript entry loaded build=20260519_restore_attack_exp_no_hp_v1`。
- fresh logs 确认玩家初始化：`hpControl=disabled attack=4 moveSpeed=4 level=0 exp=0 freePoints=0`。
- fresh logs 确认怪物初始化：
  - C 怪为 `hp=10/10 killExp=4`。
  - B 怪为 `hp=30/30 killExp=10`。
  - A 怪为 `hp=60/60 killExp=14`。
- 远程战斗验证通过：
  - 以 ATK `4` 连续攻击 C1，日志依次出现 `hp=6/10`、`hp=2/10`、死亡 `hp=0`。
  - 击杀 C1 后日志出现 `kill exp ... exp=4`，玩家经验变为 `4`。
  - 调用玩家伤害后日志为 `ignore player damage`，确认人物 HP 不参与控制。
  - 继续击杀 C2、C3 后玩家升级到 `LV1`，剩余 `EXP2`，属性点 `2`。
  - C1、C2、C3 全部击杀后，`1594492577` 消失，`1487962612` 保持可见。

## 2026-05-19 移除 SPD 属性 UI

本节为当前最新规则，优先级高于下方旧阶段记录：

- 玩家属性 UI 只保留 `LV`、`EXP`、`ATK`。
- 已删除 `SPD` 显示。
- 已删除移动速度加点按钮和移动速度加点逻辑。
- `PlayerStats` 不再保存 `moveSpeed`，也不再通过脚本属性面板修改玩家移动速度。
- 玩家 HP 控制仍保持取消。
- 攻击、等级、经验、攻击加点、怪物 HP 扣血和击杀经验逻辑保持不变。

已验证：

- `npm run build` 通过。
- 生成 Lua 中已无 `SPD`、`stage5_add_speed`、`stage5_speed_text`、`move_speed`、`moveSpeed`。
- 生成 Lua 中保留 `LV`、`EXP`、`ATK`。
- `sync-start` 已完成 build、flush 与 main lua 同步。
- 编辑器本地 RPC 读取 fresh logs 时返回 `WinError 10013`，因此本次无法远程取回新运行日志；需要编辑器侧恢复连接后再做 fresh logs 验证。

## 总体实现

已新增 `ts_src/Stage8To12System.ts`，并在 `ts_src/main.ts` 的 `GAME_INIT` 流程中初始化。

本次实现统一使用组件 ID 和文档坐标作为主依据，避免同名组件或编辑器组件自身触发能力不稳定导致逻辑失效。触碰/站立/进入区域类逻辑采用玩家位置轮询兜底；方墙撞击采用物理组件接触事件；指定怪击杀采用 `MonsterManager.OnMonsterKilled()` 监听。

## 阶段 8：方墙46与蹦床1

目标组件：

- 方墙46：ID `1925646933`
- 蹦床1：ID `1873217301`

当前实现：

- 方墙46由 `Stage7GateSystem` 监听 B12、B11、B10、A4 击杀进度控制隐藏。
- 蹦床1初始化隐藏并关闭物理，隐藏状态下没有弹力效果。
- 击杀 A10、A11、C9、C10 后，蹦床1复位、显示并启用物理/弹力。
- 方墙46不再注册撞击破坏机制，不再使用玩家等级条件。

关键日志：

- `[Stage7][GateSystem] visible target name=方墙46 id=1925646933 visible=false`
- `[Stage8To12] show 蹦床1 id=1873217301 reason=stage8 target monsters killed`

## 阶段 9：按钮开关1与机械墙5

目标组件：

- 按钮开关1：ID `1855557960`
- 机械墙5：ID `1732139556`

当前实现：

- 使用按钮开关1的文档坐标做玩家距离检测。
- 玩家进入按钮附近范围后，只触发一次。
- 触发后机械墙5隐藏并关闭物理。
- 只有玩家位置检测会触发，怪物不会触发。

关键日志：

- `[Stage9] button triggered role=... opened=...`

## 阶段 10：中式花纹砖1与箱子1

目标组件：

- 中式花纹砖1：ID `1041548108`
- 箱子1：ID `1192156288`
- 箱子1打开后显现实体：ID `1147395932`

当前实现：

- 使用中式花纹砖1文档坐标做玩家站立检测。
- 玩家连续站立满 3 秒后，箱子1隐藏并视为打开。
- 玩家中途离开，计时清零。
- 玩家在计时期间移动或被击飞，计时清零。
- 箱子1只打开一次。
- 箱子1打开后，实体 `1147395932` 显现。

关键日志：

- `[Stage10] tile leave role=... reset`
- `[Stage10] tile movement reset role=...`
- `[Stage10] box1 open role=...`

## 阶段 11：指定 A 怪击杀与箱子2

目标怪物：

- 变异蛋A7：ID `1303708057`
- 变异蛋A14：ID `1997721046`
- 变异蛋A8：ID `1327462744`
- 变异蛋A13：ID `1731296382`

目标组件：

- 箱子2：ID `1218997392`
- 箱子2显现实体：ID `1712673236`

当前实现：

- `MonsterManager` 新增 `OnMonsterKilled()` 监听出口。
- 指定四只 A 怪首次死亡后记录进度。
- 怪物复活不会清除已击杀进度。
- 四只目标全部击杀后，箱子2隐藏并视为打开。
- 箱子2打开后，实体 `1712673236` 显现。

关键日志：

- `[Stage11] target killed name=... id=...`
- `[Stage11] box2 open=...`

## 阶段 12：密码锁1、机械墙3与通关

目标组件：

- 密码锁1：ID `1965974982`
- 机械墙3：ID `1156059701`
- 胜利区域1：ID `1146983526`

当前实现：

- 密码锁1使用编辑器原生密码锁逻辑，代码监听其互动事件和运行时状态。
- 密码锁1开局不判定失效；玩家输入正确密码后，编辑器原生“解锁成功事件”`输入密码2026` 会被脚本监听，随后隐藏机械墙3。
- 脚本隐藏机械墙3后会发送 `机械墙3消失`，用于触发编辑器里配置的互动停用事件。
- 仍保留组件丢失、模型隐藏或物理失效作为兜底信号。
- `touchable=false` 会在原生密码输入界面打开时出现，不能作为密码完成依据；已删除“持续不可交互后开门”的兜底逻辑，避免未输完密码就打开机械墙3。
- 不创建自建密码 UI，使用编辑器原生输入面板。
- 玩家进入胜利区域1附近会触发通关。
- 通关调用 `role.game_win_and_show_result_panel()`。

关键日志：

- `[Stage12] legacy password ui cleanup skipped names=12`
- `[Stage12][PasswordLock] monitor ready lock=1965974982 wall=1156059701 ...`
- `[Stage12][PasswordLock] wall opened lock=1965974982 wall=1156059701 reason=...`
- `[Stage12] victory role=...`

## 改动文件

- `ts_src/Stage8To12System.ts`
  - 新增阶段 8 到 12 的机关、提示、密码、通关系统。
- `ts_src/MonsterManager.ts`
  - 新增 `OnMonsterKilled()`，供阶段 11 监听指定怪物击杀。
- `ts_src/main.ts`
  - 新增 `Stage8To12System.Init()` 初始化入口。

## 验证结果

已执行：

- `npm run build`
- `../platform/tools/bridge/eggitor-curl.sh --workspace /home/zhangyuwen/eggy_space/mt sync-start`
- 通过 local-agent 执行编辑器 `clear-logs`、`stop_game`、`run_game` 并读取日志。

结果：

- TypeScript 到 Lua 编译通过。
- `sync-start` 返回 `build_ok=true`、`flush_ok=true`、`main_lua_ok=true`、`start_ok=true`。
- 本地生成 Lua 已包含：
  - `project/ts_out/game/Stage8To12System.lua`
  - `project/ts_out/game/main.lua` 中的 `Stage8To12System.Init()`
  - `project/ts_out/game/MonsterManager.lua` 中的 `OnMonsterKilled()`
- 编辑器日志确认入口已加载最新 marker：`[mt] TypeScript entry loaded build=20260518_stage8_to_12_v1`。

历史验证缺口：

- 早期曾缺少 `lua-language-server`，因此 `npm run lint` 未能执行。
- 早期 local-agent 触发试玩后只稳定看到入口日志，后续已通过 fresh logs 和远程脚本补测阶段 8 到 12 的核心逻辑。

## 当前验收状态

阶段 8 到 12 当前为“代码已实现、编译与同步通过、编辑器入口加载确认，核心机关已远程验证”。

待实机验证：

- 阶段 8：实机确认击杀 A10、A11、C9、C10 后蹦床1的实际可跳体验。
- 阶段 9：已远程验证按钮开关1能隐藏机械墙5，实机只需确认碰撞手感。
- 阶段 10：已远程验证站砖 3 秒开箱和移动重置，实机只需确认站位容错。
- 阶段 11：已远程验证击杀 A7、A14、A8、A13 后箱子2打开并显现 `1712673236`。
- 阶段 12：通过原生密码锁1输入正确密码使组件失效后，机械墙3隐藏；进入胜利区域1后通关。

## 历史自动测试：旧等级流程验证记录

2026-05-18 更新：

- 已执行 `npm run build` 与 `sync-start`，返回 `build_ok=true`、`flush_ok=true`、`main_lua_ok=true`、`start_ok=true`。
- 已通过 `EditorAPI.game_execute(...)` 在试玩中执行自动化测试脚本。
- 初始化验证通过：
  - 玩家初始 HP 日志为 `100/100`。
  - Stage8To12 初始化成功。
  - `方墙32` 提示 `2级可过` 已初始化。
- 等级门验证通过：
  - 玩家加经验到 13 级后，`欧式方形窄门1`、`欧式方形窄门2`、`机械墙6`、`机械墙4` 均执行了解锁/隐藏日志。
  - `穿梭电门17` 执行了显式恢复可见日志。
- 阶段 9 验证通过：
  - 传送玩家到按钮开关1附近后，`机械墙5` 隐藏，日志出现 `[Stage9] button triggered ... opened=true`。
- 阶段 10 验证通过：
  - 传送玩家到中式花纹砖1并停留满 3 秒后，`箱子1` 打开，日志出现 `[Stage10] box1 open role=1`。
- 阶段 11 首轮测试发现问题：
  - 击杀 `变异蛋A7` 后，`Stage8To12System.lua` 报错：`attempt to call a nil value (global 'tonumber')`。
  - 原因：运行时沙盒中没有全局 `tonumber`，旧实现用 `for ... in TARGET_A_MONSTER_IDS` 后再 `tonumber(idText)`。
  - 修复：新增固定数字数组 `TARGET_A_MONSTER_ID_LIST`，直接遍历数字 ID，不再调用 `tonumber`。
- 阶段 11 修复后复测通过：
  - 依次击杀 A7、A14、A8、A13 后，日志出现 `[Stage11] box2 open=true`。
  - `箱子2` 隐藏，自动测试读到 `box2=false`。
- 怪物复活单测通过是旧机制记录；当前机制已改为变异蛋死亡后不复活。
- 阶段 8 方墙46撞击验证是旧机制记录；当前机制已改为击杀 B12、B11、B10、A4 后隐藏方墙46。
- 阶段 12 部分验证：
  - 靠近密码锁1可执行位置检测流程。
  - 远程脚本无法模拟玩家点击 EUI 数字按钮，因此无法由远程自动输入 `2026` 完成最终通关；需要本机实机点击密码按钮后再进入胜利区域确认。

历史结论：

- 该段记录的是 2026-05-18 的旧等级/复活流程验证，不作为当前玩法验收依据。

## 本次 Stage10 逻辑调整：箱子1打开后显现实体

2026-05-18 更新：

- 已移除 Stage10 原来的提示词逻辑描述与代码触发。
- 现在玩家连续站在中式花纹砖1满 3 秒后，箱子1隐藏并视为打开，随后实体 `1147395932` 显现。
- 初始化时实体 `1147395932` 会先隐藏，避免开局直接可见。
- 已执行 `npm run build`，编译通过。
- 已执行 `sync-start`，返回 `build_ok=true`、`flush_ok=true`、`main_lua_ok=true`、`start_ok=true`。
- 编辑器日志确认加载入口 marker：`[mt] TypeScript entry loaded build=20260518_box1_reveal_1147395932_v1`。
- 编辑器初始化日志确认实体存在并被隐藏：`[Stage8To12] hide 实体1147395932 id=1147395932 reason=stage10 init hidden`。
- 远程站砖测试通过：
  - 玩家传送到中式花纹砖1 `[7.0, 2.316, -19.0]`。
  - 连续站立后日志出现 `[Stage8To12] hide 箱子1 id=1192156288 reason=stage10 open`。
  - 随后日志出现 `[Stage8To12] show 实体1147395932 id=1147395932 reason=stage10 box1 open`。
