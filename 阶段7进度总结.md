# 阶段 7 进度总结

更新时间：2026-05-18

本总结对应 `开发阶段拆分.md` 的阶段 7：等级门与基础机关。

## 本步执行内容

已新增阶段 7 的等级门/机械墙解锁系统。玩家升级后，系统会检查当前等级，并按文档条件一次性打开对应门墙。

## 改动文件

- `ts_src/PlayerStats.ts`
  - 新增等级变化监听机制 `OnLevelChanged`。
  - 玩家升级后会通知监听者，供阶段 7 解锁系统响应。
- `ts_src/Stage7GateSystem.ts`
  - 新增阶段 7 专用系统。
  - 管理等级解锁规则和门墙打开状态。
- `ts_src/main.ts`
  - 在 `GAME_INIT` 时初始化阶段 7 等级门系统。
- `project/ts_out/**`
  - `npm run build` 后生成的 Lua 输出。

## 当前实现

等级规则：

- 玩家达到 2 级：`欧式方形窄门1`（ID `1594492577`）消失；`穿梭电门17`（ID `1487962612`）保留原功能。
- 玩家达到 4 级：`欧式方形窄门2`（ID `1050532163`）消失，并同步隐藏相邻视觉组件 `传送门1`（ID `1422072101`）。
- 玩家达到 4 级：`机械墙6`（ID `1687330069`）消失。
- 玩家达到 12 级：`机械墙4`（ID `1381759788`）打开。

实现方式：

- 优先用 `GameAPI.get_unit(id)` 按场景 Unit ID 精确查询场景物体。
- 如果 ID 查询失败，再用 `LuaAPI.query_unit(name)` 作为兜底。
- 用 `set_physics_active(false)` 关闭目标物体的物理阻挡。
- 用 `role.set_unit_visible(unit, false, false)` 按玩家隐藏目标物体，并明确不向子节点扩散。
- 对 2 级门额外设置保护目标：隐藏 `欧式方形窄门1`（ID `1594492577`）后，会立即对所有玩家恢复 `穿梭电门17`（ID `1487962612`）可见，避免重叠组件或运行时关联导致电门被连带隐藏。
- 用 `unlockedByName` 记录解锁状态，保证每个门墙只触发一次。
- 初始化时会检查当前已有玩家等级，避免系统初始化晚于等级变化时漏开。
- 初始化后每 1 秒兜底检查一次所有玩家等级，避免只依赖升级事件导致门墙漏开。

## 本次排查：2 级门未消失

玩家升到 2 级后 `欧式方形窄门1` 未按预期从画面消失，排查结果如下：

- 阶段 7 升级监听逻辑本身没有发现明显冲突：`PlayerStats.AddExp()` 在等级上升后会调用 `OnLevelChanged` 监听，`Stage7GateSystem` 会在回调中检查等级并执行解锁。
- 原实现只通过名称查询并隐藏 `欧式方形窄门1` 本体。
- 场景快照显示 `欧式方形窄门1` 同位置还叠着 `穿梭电门17`。
- 根据当前玩法要求，`穿梭电门17` 需要保留原功能；因此 2 级解锁只按 ID 精确隐藏 `欧式方形窄门1`（ID `1594492577`），不处理 `穿梭电门17`（ID `1487962612`）。
- 同步补强了 4 级门：`欧式方形窄门2` 旁边存在 `传送门1`，现在也会一并隐藏。

## 文档维护：组件 ID 补全

2026-05-18 更新：

- 已将最近一次场景快照中的现有组件名称与 Unit ID 补入 `开发阶段拆分.md` 的“场景组件 ID 索引”。
- 当前实时导出失败，原因是编辑器本地 WebSocket 端口不可连，符合编辑器已关闭的状态；本次使用的是最近一次成功导出的 `/tmp/mt_scene_export/latest_scene_units.json`。
- 阶段 7 后续继续采用“ID 优先、名称兜底”的实现方式，避免同名、重叠组件或名称查询不稳定导致门墙未正确消失。

## 本次修正：2 级门仍未消失

2026-05-18 更新：

- 实测反馈：玩家达到 2 级后，ID `1594492577` 的 `欧式方形窄门1` 仍未消失。
- 之前只验证了编译、同步和日志入口，没有实际把玩家升到 2 级验证该门消失；该验证口径不充分。
- 当前修正：2 级只处理 `欧式方形窄门1`（ID `1594492577`），不处理 `穿梭电门17`（ID `1487962612`）。
- 当前修正：隐藏目标时不再移动物体，也不再使用 `set_model_visible(false)`；改为 `set_physics_active(false)` + `role.set_unit_visible(unit, false, false)`。
- 当前修正：阶段 7 增加 1 秒一次的等级兜底检查，避免升级事件漏触发。

## 本次修正：2 级门隐藏连带穿梭电门17消失

2026-05-18 更新：

- 实测反馈：玩家达到 2 级后，`欧式方形窄门1`（ID `1594492577`）可以消失，但重叠位置的 `穿梭电门17`（ID `1487962612`）也一起消失。
- 代码排查：当前 `ts_src/Stage7GateSystem.ts` 和生成 Lua 中，2 级隐藏目标只有 `1594492577`，没有把 `1487962612` 放入隐藏列表。
- 运行链路排查：此前平台入口 `main.lua` 曾经没有清理 `require` 缓存，编辑器可能沿用过旧的 `Stage7GateSystem.lua`，旧日志里曾出现过 `1487962612` 被隐藏。
- 当前修正：平台入口 `../platform/runtime/main.lua` 已扩大 Lua `package.loaded` 清理范围，覆盖 `project/ts_out/game`、`project.ts_out.game`、`project/ts_out/common`、`project.ts_out.common` 四类模块 key。
- 当前修正：2 级规则增加 `preserveTargets`，隐藏 `1594492577` 后立刻调用 `role.set_unit_visible(1487962612, true, false)` 恢复 `穿梭电门17` 对所有玩家可见。
- 当前判断：两个组件坐标不同但高度重叠，且可能存在编辑器组合/运行时可见性关联；因此不能只依赖“不把 1487962612 写进隐藏列表”，还需要显式保护恢复。

## 本次修正：加强玩家等级监听并在 EXP 栏显示 LV

2026-05-18 更新：

- 按需求将 EXP 栏文本从 `EXP 当前/下级` 调整为 `LV 等级  EXP 当前/下级`。
- 屏幕 EXP 文本 `stage1_exp_text` 不再创建后隐藏，会随 `RefreshDisplay()` 显示同样的 `LV 等级  EXP 当前/下级`。
- EXP 栏和屏幕 EXP 文本现在创建时就带默认文本 `LV 0  EXP 0/10`，避免等待第一次刷新前空白。
- UI 刷新现在同时调用 `GameAPI.set_node_visible/set_label_text` 和 `role.set_node_visible/set_label_text`，规避动态 EUI 只按玩家 API 设置时不显示的问题。
- EXP 栏宽度从 220 调整为 360，避免 `LV 等级  EXP 当前/下级` 被裁剪。
- `PlayerStats.InitAllPlayers()` 增加 1 秒一次的玩家状态兜底刷新：玩家晚进入时自动补初始化，玩家已初始化时持续刷新 HUD/属性面板。
- `PlayerStats.OnLevelChanged()` 增加注册后的当前等级即时回放，避免阶段 7 等系统晚注册时错过已有等级状态。
- `AddExp()` 升级时继续触发等级监听，并刷新 EXP/LV 显示与属性点显示。
- 排查发现一次 TSTL 编译细节：自定义 `GlobalUiApi` 接口必须标注 `this: void`，否则会被编译为 Lua 冒号调用并多传 self；已修正为点调用。
- 根据新方案，属性面板中新增独立等级文本 `stage5_level_text`，放在 EXP 框上方，显示 `LV 等级`。
- EXP 框内文本改回只显示 `EXP 当前/下级`，避免等级和经验挤在同一个框里导致不显示或裁剪。

## 验证结果

已执行：

- `npm run build`
- `../platform/tools/bridge/eggitor-curl.sh --workspace /home/zhangyuwen/eggy_space/mt sync-start`
- `../platform/tools/bridge/eggitor-curl.sh --workspace /home/zhangyuwen/eggy_space/mt status`
- `../platform/tools/bridge/eggitor-curl.sh --workspace /home/zhangyuwen/eggy_space/mt game-stop`
- 通过 `local-agent` 执行 `EditorAPI.run_game()` 并读取日志通道探针。
- 通过 `../platform/tools/export_scene_units/export_scene_units.py --mode local-agent` 导出当前场景单位快照。

结果：

- 编译通过。
- 最新 `sync-start` 返回 `build_ok=true`、`flush_ok=true`、`main_lua_ok=true`、`start_ok=true`。
- 最新同步的入口 `main.lua` 大小为 523 字节，已包含更宽的 Lua 模块缓存清理逻辑。
- backend 状态正常，`busy=false`、`tunnel.running=true`、`ws_clients=1`。
- 当前本地编辑器日志通道返回 `WinError 10013`，因此这一步只能确认构建、flush、入口下发和启动请求成功，不能从日志尾部可靠确认本次新 marker 已出现。
- 本次 LV/EXP 修改已通过 `npm run build` 生成到 `project/ts_out/game/PlayerStats.lua`，生成 Lua 中已包含 `LV ... EXP ...`、`RefreshAllPlayers()` 和 `stats tick registered` 逻辑。
- 注意：当前 `sync-start` 返回的 `logTail` 仍夹带旧阶段 7 日志片段，不能据此判断最新 Stage7/PlayerStats 运行状态；需要以实机画面和新日志 marker 为准。
- 最新排查确认：本地 `project/ts_out/game/main.lua` 已包含 marker `[mt] TypeScript entry loaded build=20260518_lv_exp_v2`，`PlayerStats.lua` 已包含 marker `[Stage1][PlayerStats] InitAllPlayers build=20260518_lv_exp_v2`。
- 但编辑器返回的日志仍停留在旧 marker `[mt] TypeScript entry loaded`，且 `game-stop` 返回的也是旧日志；这说明当前编辑器试玩没有真正重启到最新 TS 输出。需要先停止当前试玩或重启编辑器后再运行，才能看到 LV/EXP UI 的最新实现。
- 最新同步状态复查：backend `/status` 正常，`ws_clients=1`、`busy=false`、`tunnel.running=true`。
- 最新同步状态复查：backend 日志显示 `flush_code` 成功，`files_total=22`、`sent_total=19`，`sync_main_lua` 成功，入口 `main.lua` 已下发为 980 字节版本。
- 最新同步状态复查：本地生成文件已包含 `stage5_level_text`、`build=20260518_lv_exp_v2`、`InitAllPlayers build=20260518_lv_exp_v2`、`init end tick=` 等新 marker。
- 当前阻塞：通过 local-agent 调用本机 `editor-cli status/logs/exec` 均失败，错误为 `Cannot connect to ws://localhost:19836: [WinError 10013]`。因此当前无法由远端确认或控制编辑器运行态。
- 当前判断：代码文件已构建并 flush 到同步通道，但当前编辑器试玩实例仍在运行旧 Lua，或当前打开的编辑器不是正在接收 flush 的那个运行态；需要在本机手动停止试玩/重启编辑器后再启动，确认日志出现 `build=20260518_lv_exp_v2`。
- 后续实测反馈：重启编辑器后属性数值框全部不见。
- 排查结果：入口 `main.lua` 中为了清理 Lua require 缓存使用了 `package.loaded`，但当前编辑器 Lua 环境里 `package` 为 nil，导致入口报错 `main.lua:4: attempt to index a nil value (global 'package')`，游戏 TS 入口未继续执行，所以数值框全部没有创建。
- 修复：`../platform/runtime/main.lua` 已恢复为最小安全入口，只打印、`require("project/ts_out/game/main")`、再打印，不再使用 `package.loaded`。
- 同步验证：最新 `sync-start` 后日志已出现 `[mt] TypeScript entry loaded build=20260518_lv_exp_v2`、`[Stage5][PlayerStats] stats panel created`、`[Stage1][PlayerStats] stats tick registered id=1`、`[Stage7][GateSystem] init end tick=4`，说明编辑器已加载最新 TS 输出，数值框创建流程已恢复。
- 按新布局要求，`LV` 独立显示在 EXP 框上方，间距使用与 EXP/HP 之间相同的 `ATTR_PANEL_GAP=10`。
- 删除多余的 `LV ... EXP ...` 组合文本；当前屏幕 EXP 与 EXP 框内只显示 `EXP 当前/下级`，只有 EXP 框上方的 `stage5_level_text` 显示 `LV 等级`。
- 最新同步验证：日志出现 `[Stage5][PlayerStats] stats panel created`、`[Stage1][PlayerStats] stats tick registered id=1`，入口仍为 `[mt] TypeScript entry loaded build=20260518_lv_exp_v2`，说明数值面板创建流程正常。
- 再次同步确认：为验证最新布局已进入编辑器，入口 marker 更新为 `build=20260518_lv_exp_layout_v3`。`sync-start` 日志已出现该 marker，并出现 `[Stage1][PlayerStats] InitAllPlayers build=20260518_lv_exp_layout_v3`，说明编辑器已加载本次“LV 独立放在 EXP 上方”的版本。
- 启动返回的日志尾部包含 `start_move_to_pos_with_threshold` 的 Lua API 错误，位置在 `MonsterManager.lua` 的追击/返回逻辑；该问题属于阶段 6 怪物 AI 运行时风险，不是本次阶段 7 代码的编译错误。
- 运行时日志确认已进入游戏 TS 入口，并执行到 `[Stage7][GateSystem] init end`。
- 场景快照确认以下阶段 7 目标物体存在：
  - `欧式方形窄门1`：ID `1594492577`
  - `欧式方形窄门2`：ID `1050532163`
  - `机械墙4`：ID `1381759788`
  - `机械墙6`：ID `1687330069`

## 当前验收状态

阶段 7 当前为“构建和同步通过，2 级门保护逻辑已下发，等级触发效果待实机复测”。

代码层面已覆盖：

- 未达到等级时不会主动打开对应门墙。
- 达到等级后会执行一次打开/消失逻辑。
- 触发后状态不会因后续重复等级检查而重复执行。

仍需游戏内验证：

- 玩家达到 2 级后，`欧式方形窄门1`（ID `1594492577`）消失。
- 玩家达到 2 级后，`穿梭电门17`（ID `1487962612`）仍保持可见和原功能。
- `机械墙4` 是否需要播放机关打开动画，而不是直接隐藏。
- 玩家实际升到 2、4、12 级时，门墙是否按预期从客户端画面消失并取消阻挡。

## 遗留问题

- 现有 `main.ts` 中还有一个旧的 `initStage7PlayerSkill`，它不是执行文档里的阶段 7 内容，只是给玩家装预设技能；后续如果严格按阶段编号整理，可以重命名日志或移出阶段编号，避免混淆。
- 阶段 7 依赖阶段 4 的升级流程。由于阶段 4 的 EXP HUD 仍有显示缺口，试玩时可能不容易直观看到等级变化。

## 本次修正：重新编译 Lua 并排查 LV 文本重复

2026-05-18 更新：

- 已重新执行 `npm run build`，旧 TS 输出已编译成最新 Lua，生成文件包含 `build=20260518_lv_exp_layout_v5`。
- 已执行 `sync-start`，返回 `build_ok=true`、`flush_ok=true`、`main_lua_ok=true`、`start_ok=true`，说明 Lua 与 `main.lua` 已下发到编辑器同步目录。
- 通过 local-agent 清空编辑器日志后执行 `EditorAPI.stop_game()` + `EditorAPI.run_game()`，日志确认编辑器实际加载：
  - `[mt] TypeScript entry loaded build=20260518_lv_exp_layout_v5`
  - `[Stage1][PlayerStats] InitAllPlayers build=20260518_lv_exp_layout_v5`
  - `[Stage5][PlayerStats] stats panel created`
- 本次确认“编辑器没有变”的主要原因不是 flush 没同步，而是运行时动态 EUI 残留/重复创建造成的视觉干扰；`sync-start` 的 logTail 也会夹带旧日志，不能单独作为最新运行态依据。
- 顶部屏幕 EXP 文本 `stage1_exp_text` 已停止创建和刷新，避免旧版本 `LV ... EXP ...` 与左侧 EXP 框上方的 `stage5_level_text` 同时出现。
- 曾尝试用 `LuaAPI.query_ui_node("stage1_exp_text")` 清理旧动态 EUI，但实测该返回值传给 `role.set_node_visible/set_label_text` 会报“节点类型不正确”；该方案已撤回，避免每秒刷新报错。
- 最新 stop/start 验证日志中未再出现上述 `stage1_exp_text` 节点类型错误。

当前 UI 结论：

- 左侧属性面板保留唯一等级文本：`stage5_level_text`，显示 `LV 等级`，位于 EXP 框上方。
- EXP 框内只显示 `EXP 当前/下级`。
- 顶部屏幕 EXP 文本不再由代码创建；如果编辑器画面仍看到旧顶部 LV，大概率是旧试玩运行态残留，需要停止试玩后重新运行，或重启编辑器再进入试玩。

## 本次修正：LV 与 EXP 间距改为 120 并记录 19:9 屏幕位置

2026-05-18 更新：

- 按需求将 `LV 0` 保持在 EXP 框上方，并把 LV 文本底部到 EXP 框顶部的垂直间距改为 `120`。
- 为避免 LV 跑出屏幕，属性面板整体下移：
  - `LV` 顶部：`40`
  - `LV` 底部：`68`
  - `EXP` 框顶部：`188`
  - `LV` 到 `EXP` 间距：`120`
  - 左侧面板 left：`260`
  - 面板中心 x：`440`
- 当前按 19:9、虚拟高度 1000 计算，虚拟宽度约为 `2111`。
- 已增加运行时布局日志：`layout=lv_exp_gap_120 aspect=19:9 virtualWidth=2111 levelTop=40 levelBottom=68 expTop=188 gap=120 left=260 centerX=440`。
- 已更新 marker 到 `build=20260518_lv_exp_layout_v7`。
- 已执行 `npm run build` 和 `sync-start`，并通过 local-agent 清日志后执行 `EditorAPI.stop_game()` + `EditorAPI.run_game()` 验证编辑器实际加载 v7。

## 本次修正：按黄色 EXP 框反推 LV 透明文本框位置

2026-05-18 更新：

- 确认 `LV 0` 是透明 `ELabel` 文本节点，黄色 EXP 框是 `stage5_exp_panel` 按钮节点。
- 按需求只以黄色 EXP 框为参照：`LV 文本框 top = EXP 黄色框 top - 120 - LV 文本框高度`。
- 为减少透明文本框对肉眼位置的影响，`LV` 文本框高度从 `28` 收紧到 `24`。
- 当前同步后的实际布局日志：
  - `levelTop=44`
  - `levelBottom=68`
  - `expTop=188`
  - `gap=120`
  - `left=260`
  - `centerX=440`
- 已更新 marker 到 `build=20260518_lv_exp_layout_v8`。
- 已执行 `npm run build`、`sync-start`，并通过 local-agent 清日志后执行 `EditorAPI.stop_game()` + `EditorAPI.run_game()` 验证编辑器实际加载 v8。

## 本次修正：按 LV 可见文字中心对齐黄色按钮

2026-05-18 更新：

- 根据反馈，进一步明确不按透明文本框边界算间距，而按 `LV` 可见文字位置与黄色 EXP 按钮框计算。
- 当前黄色 EXP 按钮框 top 固定为 `188`。
- 当前 `LV` 透明文本框高度为 `24`，文本中心按 `levelTextCenter` 记录。
- 当前计算方式：`levelTextCenter = expTop - 120`。
- 同步后的源码/生成 Lua 计算结果：
  - `levelTop=56`
  - `levelBottom=80`
  - `levelTextCenter=68`
  - `expTop=188`
  - `levelTextCenter` 到黄色 EXP 按钮 top 的距离：`120`
- 已更新 marker 到 `build=20260518_lv_exp_layout_v9`。
- 已执行 `npm run build` 与 `sync-start`，backend 状态正常，`ws_clients=1`。

## 本次修正：放大 LV 字体

2026-05-18 更新：

- 按需求只放大 `LV` 文本字号，EXP/HP/ATK/SPD 字号保持 `24` 不变。
- `LV` 字号从 `24` 调整为 `32`。
- 为避免大字号被透明文本框裁剪，`LV` 透明文本框高度从 `24` 调整为 `36`。
- 仍按黄色 EXP 按钮框计算位置，保持 `levelTextCenter = expTop - 120`：
  - `levelTop=50`
  - `levelBottom=86`
  - `levelTextCenter=68`
  - `levelFont=32`
  - `expTop=188`
  - `gap=120`
- 已更新 marker 到 `build=20260518_lv_exp_layout_v10`。
- 已执行 `npm run build`、`sync-start`，并通过 local-agent 清日志后执行 `EditorAPI.stop_game()` + `EditorAPI.run_game()` 验证编辑器实际加载 v10。

## 本次修正：玩家初始 HP 调整为 100，并给方墙32增加 2 级提示

2026-05-18 更新：

- 按需求将玩家初始血量从 `20/20` 调整为 `100/100`。
- 修改位置：`ts_src/PlayerStats.ts` 的 `DEFAULTS.maxHp` 与 `DEFAULTS.hp`。
- 已在 `方墙32` 上增加公告板文字提示：
  - 名称：`方墙32`
  - ID：`1523845553`
  - 位置：`[-18.5, 2.316, 9.0]`
  - 文字：`2级可过`
  - 字号：`30`
- 实现位置：`ts_src/Stage8To12System.ts` 的 `setupWall32Hint()`，在阶段 8-12 系统初始化可见性时执行。
- 当前 API 排查结果：公告板接口只暴露了 `set_billboard_text`、`set_billboard_font_size`、`set_billboard_text_color`，没有找到公告板字体名/字体 key 设置接口；因此本次已设置文本和字号，字体使用公告板默认字体。如果后续必须强制宋体，需要改用可设置 `FontKey` 的 EUI/SceneUI 或找到编辑器侧字体资源 key 映射。
- 已更新 marker 到 `build=20260518_hp100_wall32_hint_v2`。
- 已执行 `npm run build` 并生成 Lua：
  - `project/ts_out/game/PlayerStats.lua` 已包含 `maxHp = 100`、`hp = 100`。
  - `project/ts_out/game/Stage8To12System.lua` 已包含 `方墙32`、`2级可过`、`set_billboard_font_size(30)`。
  - `project/ts_out/game/main.lua` 已包含入口 marker `build=20260518_hp100_wall32_hint_v2`。
- 已执行 `sync-start`，返回 `build_ok=true`、`flush_ok=true`、`main_lua_ok=true`、`start_ok=true`。
- 通过清日志后的编辑器日志曾确认 v1 进入 `GAME_INIT`，并出现：
  - `[Stage1][PlayerStats] InitAllPlayers build=20260518_hp100_wall32_hint_v1`
  - `[Stage1][PlayerStats] init role=1 hp=100/100 ...`
  - `[Stage7][Wall32Hint] billboard name=方墙32 id=1523845553 text=2级可过 ... size=30`
- v2 额外修正：阶段 12 的 `胜利区域1` 是用于通关距离检测的区域目标，不再初始化时调用 `role.set_unit_visible()`，避免该区域触发 `set_unit_visible` 引擎内部错误；生成 Lua 已确认改为日志 `[Stage12] victory zone ready ...`。
- 当前验证限制：v2 同步后，local-agent 的 `EditorAPI.stop_game()` / `EditorAPI.run_game()` 出现 `Timeout waiting for response to exec_lua`，因此 fresh-log 未能再次读到 v2 的 `GAME_INIT` 日志；本地 Lua 与 backend 同步状态已确认正常，需编辑器端恢复命令响应后再做实机画面复核。
